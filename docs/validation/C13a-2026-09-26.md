# C13a acceptance — portable parser and cooperative runtime

C13a is accepted; parent C13 remains Pending. `src/core/parser/metadata.ts` now owns body/property-reference grammar, plain DTOs and explicit cooperative clock/yield/cancellation. Production worker/fallback and collectors use that owner. [Machine-readable evidence](C13a-2026-09-26.json) records the exact final artifact and native results.

## Review and corrections

Compared the actual accepted `6af77d0` parser with the delivery. Eight moved function declarations are identical under AST normalization. Cooperative scans/grammar are unchanged apart from runtime injection and the explicit missing-runtime guard. Every frozen oracle case also matches the actual accepted parser: **33 body cases and five property cases**. This verifies the oracle's origin independently of the offline delivery.

Expanded cooperative oracle coverage from ten selected cases to all 33. Added the parser dependency row to architecture documentation and fixed trailing whitespace. Core has no SDK, Node/DOM/browser global, storage or default timer dependency. The serialized worker function remains self-contained. The unused `MetadataParseWorker` class had no production callers; its compatibility name now delegates to MetadataParser and retains no separate grammar.

`fieldParser.ts` retains metadata merge, host linkpath resolution/deduplication and the historical cooperative signature. It supplies the same Date clock/renderer timer through the new runtime. Pure consumers import core directly. Cache shapes/keys, worker count, iOS policy, settings/schemas/UI/CSS/commands and graph semantics are unchanged. Evidence/resolver portability remains C13b; full compiler/file binding C13c; other host facades retain their documented later owners.

## Automated evidence

- Clean `npm ci`, `npm run verify` and final `npm run verify:obsidian` passed on **Node 22.22.2 / npm 10.9.7**. Architecture: 28 roots/69 reachable/zero violations; seven architecture, 22 restricted-core, 55 aggregate Node and four browser tests. Unchanged indexing assertions 1–68/P1–P17 and watchdog/placeholder checks passed. Actual installed Obsidian types/build passed. Lint: zero errors, same 24 legacy warnings. Whitespace check passed.
- Clean-process parser tests load with no Obsidian/window/DOM/Worker shim, cover all frozen synchronous/cooperative cases, serialize worker core into a closure-free realm, and prove clock budgets, post-await/dense-scan cancellation and explicit-runtime failure. Existing strict parser/heartbeat thresholds remain unchanged.
- In real Obsidian **1.14.2**, both the actual desktop worker and a private forced-workerless MetadataParser matched all 33 accepted/frozen body cases. Both also matched the accepted parser on a generated **950,000-byte note**, yielding 48 URLs. Both cancelled a 16 MiB obsolete parse with `MetadataParseCancelledError`, resumed correctly and were disposed. Published graph identity stayed unchanged during direct parsing.
- Actual source edits exercised body fields/URLs with Date/type/alias/tag/visual metadata: **one incremental patch, zero full rebuilds**, then exact declaration equality with a clean rebuild. Temporary notes were deleted. Final production bundle has no SDK accessor; fresh full compilation retains **20,701 nodes, 1,225,746 directed neighbor entries and 715,030 declarations**. Complete canonical declaration hash matches accepted C12c: `587e33e9a4eba5e6804f7348c39aa293dbe5ca0f7c8c1c4330520569be85bb57` (generated IDs omitted, multiplicity retained). Readiness/state fences reject preview or changed-state captures.
- Native command/render/error smoke passed. Index ready, hydration settled, no captured JavaScript errors, temporary accessors/controllers/fixtures absent and original background throttling true. Source/final bundle contain no review hook.

Final `main.js`: `27ee495ca61e5b073a8b4be90572125965ce4bade24372836c6ba82e512e1627`. Manifest/CSS hashes remain unchanged and are recorded in JSON.

## Performance and validation limits

Valid final foreground warm build after fresh renderer reload: **7.806 s**, zero body reads/cached reads/parser calls and 79 body-cache batches. Heartbeat max/p95 **65/32 ms**, zero hidden samples. Sampled heap before/after ~825/1,291 MB. These are single observations, not statistical performance parity or peak/device memory.

The composite native parser-parity probe reported maximum heartbeat 1,337.5 ms/p95 32.6 ms while also executing synchronous accepted baseline scans and comparing outputs. It does not isolate production worker/fallback responsiveness and is not credited as a responsiveness bound. Existing strict portable parser timer tests passed unchanged.

After renderer reload, readiness initially timed out while hidden/background-throttled. The driver mistakenly continued after failed readiness, producing an invalid warm probe, an empty preview declaration hash and cleanup assertion failure. None is credited as acceptance. Authoritative readiness/state fences and sequential prerequisites were applied; final fresh graph comparison and cleanup passed. A temporary throttle override completed diagnostic initialization; its original true policy was restored before final warm measurement/hash/cleanup. Diagnostic/background timings are excluded.

Forced desktop workerless parsing proves the actual fallback code path, not physical iOS/WebView memory or touch behavior. Physical-device/cold performance coverage remains pending in C00. No parser pool or platform strategy change is claimed.

## Manual tests and next handoff

**No manual test required for C13a.** Exact accepted-source/oracle comparisons, native worker/fallback cancellation/resume, real source edits and the final graph hash cover this extraction; UI/platform policy is unchanged.

Next is **C13b**, a Strong offline-agent candidate for evidence/resolver algorithms and narrow semantic types. The transient [HANDOFF.md](../../HANDOFF.md) is overwritten for that slice; parent C13 closes only after C13b and C13c acceptance. Rollback restores parser location/imports/facades/tests/architecture declarations without any user-data migration.
