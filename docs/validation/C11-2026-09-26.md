# C11 acceptance — normalized source input contract

C11 is accepted as an **unused contract/fixture checkpoint**. The authoritative production collectors, compiler/resolver, graph publication, indexing and UI remain unchanged. [Machine-readable evidence](C11-2026-09-26.json) records the source revision before acceptance and shipped artifact hashes.

## Review and corrections

Traced all current source families in GraphBuilder, metadata/parser, evidence and resolver code. The normalized fixture preserves exact declaring-source/target multisets and occurrence multiplicity for resolved/unresolved host links, frontmatter/inline ontology, body URLs, Date properties and file/tag topology. The existing graph/scene golden is unchanged. URL origins remain derived; shared URLs retain each declaring note; image-only links retain separate reconciliation inputs.

Main-agent fixes preserve optional origins for malformed URLs, separate frontmatter/inline metadata values, name-only field discovery, explicit per-note contribution ownership for tag membership, unresolved Date target entity facts, per-target host counts across headings, physical CRLF source ranges, body URL labels and attachment names. The oversized image fixture was replaced by a 1×1 PNG. Six dedicated tests cover these inputs, clean-process execution without Obsidian/window, endpoint completion, a 600-occurrence source spanning batches, opaque/case-distinct/pathless identities, stale generations/snapshots, sequence gaps, oversized batches and post-final rejection.

## Automated evidence

- Clean `npm ci` using required Node **22.22.2** and the unchanged lockfile passed.
- `npm run verify` passed: architecture self-tests/checker (23 migrated roots, 64 reachable files, zero violations), ES2021 core typing with no DOM/Node ambient types, 17 core tests including six C11 tests, official Obsidian ESLint, existing indexing/scene/incremental/watchdog assertions, 46 aggregate Node tests, four real browser tests and production TypeScript/build.
- ESLint: zero errors; the same 24 legacy sentence-case warnings.
- Initial sandboxed Chrome launch was blocked; the final browser tests passed with local Chrome process access. No assertions were skipped or weakened.
- `git diff --check` and the complete staged `git diff --cached --check` passed before commit.
- All three final shipped artifacts are **byte-identical to accepted C10**: main.js, styles.css and manifest.json hashes match the C10 acceptance report. No production module imports the new source contract.

## Limits and manual checks

**No manual check required.** C11 changes unused contracts and fixtures, and the shipped plugin is unchanged. No new Obsidian/native execution is claimed; C10 host evidence remains prior evidence. C00 physical-device/cold-performance coverage remains open.

The 256-record cap bounds count, not raw payload bytes. C12/C13 must preserve byte budgets and avoid duplicating dense raw values per target. Cursor helpers check only stream order/boundary/finality; entity completeness and valid compilation remain producer/compiler responsibilities. The tiny fixture producer deliberately uses limited parsing/resolution, deterministic test IDs and small arrays; it is not a production collector, Obsidian resolution oracle or memory benchmark.

No persisted key, schema, resource subscription, UI copy or runtime behavior changed. Rollback removes the C11 contract, tests/fixtures, script registrations and corresponding documentation.

Next is **C12a**, a scoped offline-agent candidate. The transient [handoff](../../HANDOFF.md) covers file/container/tag full-build collection first; the main agent owns applicable host verification on return. C12b/C12c remain necessary for parent C12 completion.
