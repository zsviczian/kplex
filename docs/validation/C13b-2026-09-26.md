# C13b acceptance — portable evidence and resolver

C13b is accepted; parent C13 remains Pending until C13c. Relationship enums/roles, generic relation flags, evidence storage/precedence/application and classification/resolution now have single portable owners under `src/core/graph/`. Legacy imports and numeric values remain stable. [Machine-readable evidence](C13b-2026-09-26.json) includes source/artifact hashes and native results.

## Review and corrections

The evidence module is text-identical to accepted `faca72d` except imports and generic relation types. Independently bundled the actual accepted modules and ran the delivered frozen oracle against them: exact match. The mandatory new core suite matches that same oracle, including IDs/multiplicity, inverse perspectives, precedence, classification and complete explanations.

Corrected three store-test assertions which referenced `Base/A.md` and `Base/Z.md` instead of the test's `A.md` and `Z.md`. Full verification then exposed missing new-module compilation in the indexing harness; added all three core graph modules to its explicit list. Re-ran the entire lane successfully. No golden, timing threshold or lint rule was relaxed.

Resolver cooperative policy retains the 7 ms budget, batch cancellation and 256-record progress cadence, and checks lifetime before/after yields. Its host facade selects the renderer timer/clock. Target binding and publication remain outside core. Restricted TypeScript, architecture and clean-process imports establish absence of SDK/DOM/Node globals; explicit path-keyed compatibility APIs do not derive meaning from opaque IDs. C13c owns normalized entity/materialization binding and full compilation.

Existing English summaries/source labels/suppression text moved unchanged and were independently compared. L01 still owns complete localization and structured reason/format separation; this checkpoint does not complete that obligation. No settings/defaults/schema/cache/snapshot/UI/CSS/command/worker/platform policy change or persisted-data migration occurred.

## Automated and native evidence

- Required **Node 22.22.2 / npm 10.9.7**, clean `npm ci` (317 packages), `npm run verify`, final `npm run verify:obsidian` and whitespace checks passed. Architecture: **31 roots / 72 reachable / zero violations**; seven architecture, 34 restricted-core, 67 aggregate Node and four browser tests. Existing indexing 1–68/P1–P17 and watchdog/placeholder assertions remain unchanged. Real installed Obsidian declarations/build passed; official lint has zero errors and the same 24 legacy warnings. These runs replace the offline delivery's unavailable dependency/build/native lanes.
- Store tests cover fork isolation, tombstones, counts/IDs, path-indexed/global removal, rename, virtual inverse views, compaction/cancellation and pair/full/cooperative agreement. Fake scheduling proves budget/progress, batch cancellation and cancellation after awaited yield; caller-supplied target identity remains intact. The representative 5,000-pair bound passed unchanged.
- In real **Obsidian 1.14.2**, a temporary hub indexed 600 distinct body URLs plus conflicting Parent/Child and directional Hidden facts. Each initial/edited/renamed/rebuilt phase matched **24 complete explanations** against the actual accepted resolver, using both inferred-friend modes. The 600-to-650 URL edit used **one incremental patch and zero rebuilds**. Rename left no declaration owned by the old source. All renamed declarations, with generated IDs omitted, exactly matched a clean rebuild.
- A private instance of the actual production GraphBuilder staged a changed high-degree body and cancelled after a forced awaited yield once its evidence fork existed. Result was cancelled with zero semantic commits; published state, evidence, pages, neighbor references, counts and source declarations remained unchanged. This is scoped cancellation evidence, not completion of C14's whole publication matrix.
- Removed the temporary hub/rename notes and reviewer accessor, restored original Electron background throttling, and deployed the exact final production bundle. Fresh full compilation retained **20,701 nodes / 1,225,746 directed neighbor entries / 715,030 original declarations**. Complete canonical declaration SHA-256 matched accepted baseline: **`587e33e9a4eba5e6804f7348c39aa293dbe5ca0f7c8c1c4330520569be85bb57`**. Generated IDs omitted; multiplicity preserved. Readiness/full hydration and captured-state fences passed.
- Final command/render/error smoke passed. No captured JavaScript errors, reviewer accessor/globals or C13b fixture files remained; readiness settled and original throttling was true. Source/final bundle scans found no review hooks. Final `main.js` SHA-256: **`3ac37bc949e9cc407c7999cd4ad8f46c2b7e54c7a37dca66d1c95a9e925517b0`**. Manifest and CSS hashes are unchanged and recorded in JSON.

## Performance and limits

Fresh-renderer foreground warm observation: **8.561 s**, zero body reads/cached reads/parser calls, 79 body-cache batches, maximum/p95 heartbeat **80.4/33.8 ms**, zero hidden samples. Sampled heap before/after ~824/1,193 MB. These are single observations, not statistical performance parity, actual-paint latency or peak/device memory. Prior C13a fresh observation was 7.806 s; no statistically meaningful comparison is claimed.

A prior warm observation after repeated review allocations took 21.301 s with sampled heap ~1,445/1,615 MB and maximum heartbeat 1,289.7 ms. It is retained in the report, not substituted for the fresh-renderer observation or claimed as an isolated resolver benchmark. A diagnostic warm rebuild with throttling temporarily disabled is separately recorded and excluded from normal performance evidence. Strict portable timing tests passed unchanged.

Initial native readiness timed out while hydration continued; that failed precondition is not credited as acceptance. Later authoritative readiness passed. A saved temporary throttle override supported semantic diagnostics and was restored before final production measurements. The first explanation driver assumed root-level Note D/Note X paths, while the host resolved nested fixture paths; corrected it to consume explicit declaring-target paths and re-ran all phase assertions. Corrected the copied cleanup driver to check the exact C13b fixture paths. No production behavior change was needed for these review-driver issues.

Physical-device/cold performance coverage stays open under C00. No worker pool, physical iOS or touch result is claimed.

## Manual tests and next handoff

**No manual test required for C13b.** Accepted algorithm/oracle comparisons, actual native high-degree edit/rename/cancellation, explanation and final whole-graph parity cover this extraction; UI/platform behavior is unchanged.

Next: **C13c, Strong offline-agent candidate**. The transient [HANDOFF.md](../../HANDOFF.md) has been overwritten for full compilation over normalized facts and production adapter binding. Parent C13 remains Pending. Rollback restores evidence/resolver/type locations, facades, tests and docs without a data migration.
