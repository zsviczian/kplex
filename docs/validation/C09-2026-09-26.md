# C09 reviewer acceptance

The definitive status is **Done** in [Refactor plan.md](../../Refactor%20plan.md). Machine-readable source/artifact identity and host assertions are in [C09 evidence](C09-2026-09-26.json).

## Review corrections

The delivered search read port was appropriately small, but its consumer still imported Obsidian transitively through `FuzzySearchInput`/`ObsidianIcon`. The actual feature now lives in architecture-checked `src/ui/features/SearchBox.tsx` and renders portable `FuzzySuggester`. App supplies the original icon and portal/topbar selectors; the old SearchBox path re-exports the feature. No input handler, layout/CSS, modal or device routing changed.

Search hits now carry an explicit secondary caption alongside the configured label and plain node view. The adapter preserves the old `page.path` caption; the consumer does not infer a path from an opaque ID or substitute a potentially different physical file facet. Search delegates existing ranking, visibility, entry-point ordering and prefix caching to GraphIndex. It maps only returned hits (the production caller requests 24), without a full graph copy or new cache.

The existing publication/presentation revision invalidates the memoized open query. Shared metadata arrays are readonly views, not frozen snapshots. Hidden mounted views catch up through the existing reveal notification. Activation resolves the exact opaque ID at the App boundary before invoking the existing navigation callback.

## Automated verification

Node **22.22.2**, clean `npm ci`, full `npm run verify`, `npm run verify:obsidian`, and `git diff --check` passed. The host runner installed/reloaded the exact production bundle and compared all three artifact hashes. Obsidian ESLint reports zero errors and 24 existing sentence-case warnings.

Portable checks include the restricted core type lane, architecture imports, a plain adapter fixture covering ordering/labels/secondary captions, and an actual browser test of the production feature without Obsidian stubs. That test proves localized copy, icon injection, ranked plain hits, a 24-hit query limit, memoization, same-query revision refresh retaining focus, opaque-ID activation, deleted-result removal and portal cleanup. Existing search fixtures retain responsibility for legacy ranking semantics.

Live Obsidian **1.14.2 (installer 1.14.0)** on desktop **996×795**, in explicitly selected `kplex-test`, used the existing 20,701-node synthetic vault. Actual displayed order/labels/captions matched the unchanged legacy query/title oracle for empty entry points (24 results), `Note` (11), and URLs (24). Two disposable notes verified ordinary index notifications after creation, rename and alias edits: the open query updated captions/results while retaining query and focus. Enter centered the selected result and cleared the query.

Deletion deliberately preserves legacy placeholders. The first strict disappearance assertion failed because placeholders were enabled. Inspection confirmed the existing dematerialization contract; the corrected test passed for the same-query real-file-to-placeholder caption transition, then for disappearance when placeholders were hidden. No deletion policy was changed. Early test-controller string-transport and navigation-cleanup errors were corrected; none represented a production failure. A hidden renderer delayed a cleanup rebuild, so the test window was foregrounded; no timing comparison is claimed.

All temporary files and placeholder nodes were removed, visibility settings and navigation restored, node cardinality returned to **20,701**, readiness was green, and no JavaScript errors were captured. A forced rebuild removed test-only ghosts after fixture deletion; search itself does not trigger that rebuild. No debug globals/wrappers or artificial delays ship.

## Manual validation and next checkpoint

No manual test is required for C09 acceptance. The changed portable boundary and publication refresh passed browser and live-host behavioral checks, and native input/responsive mechanics are preserved. These checks do not certify physical-device touch, software keyboards or large-vault mobile performance; that existing coverage remains open.

**C10 is a Strong agent candidate without Obsidian access.** Its transient assignment is in `HANDOFF.md`. It must extract real lazy property/predicate/lens behavior and leave host acceptance to the reviewing main agent. Search implementation extraction remains C17; other concrete graph readers are still listed in [Architecture](../ARCHITECTURE.md).
