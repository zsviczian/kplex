# C12c acceptance — complete production source collection

C12c and parent C12 are accepted. Date/Daily Notes, body URL/origin inputs, presentation links/counts, graph-relevant metadata and field discovery now cross the production normalized-source boundary. Full builds and cooperative patches share the collector; GraphBuilder/resolver retain semantic ownership. [Machine-readable evidence](C12c-2026-09-26.json) contains exact source/artifact hashes and native results. Compiler/parser portability remains C13.

## Review and corrections

Removed the delivered second whole-vault resolved-link count map. The adapter now reads exact counts only for each current note's visual targets; the compiler discards its per-note reconciliation map afterward. Restored established `inlineFields` value ownership independently from occurrence-based discovery, preserving accepted cache inputs where these surfaces differ. External presentation Markdown targets retain exact whitespace; discovery normalizes physical names; zero URL line preserves legacy absent provenance. Relation work uses existing time budgets rather than a forced timer every 32 records. Skipped field scans also checkpoint.

Aliases/tags remain typed strings. Configured note-type/primary-tag inputs carry only plain scalar/array projections. Unsupported objects/nested values use a non-null plain marker without retaining contents or SDK instances; this preserves the legacy rule that an unsupported frontmatter value blocks inline fallback. The compiler still unwraps and selects precedence. No arbitrary unrelated frontmatter values enter semantic records.

Added accepted map/extractor parity, contradictory map/occurrence inputs, host-object/precedence, exact visual count, dense 1,800-fact Date/URL/image batching and skipped-scan cancellation checks. Existing graph/evidence/scene goldens and timing thresholds are unchanged. The [source inventory](../NORMALIZED_SOURCE_CONTRACT.md#c12c-completeness-and-retained-host-seams) names every remaining acquisition/file-binding/parser/cache/scheduling seam and its later owner. No settings, persistence schema, UI, CSS, commands or worker/device policy changed.

## Automated evidence

- Clean `npm ci`, `npm run verify` and final `npm run verify:obsidian` passed on Node **22.22.2**, npm **10.9.7**. Architecture: 27 roots/68 reachable/zero violations; six architecture, 17 restricted-core, 50 aggregate Node and four browser tests. Existing indexing assertions 1–33/P1–P17 passed; real installed Obsidian types/build passed. Official lint has zero errors and the same 24 legacy warnings. Whitespace checks passed.
- Real Obsidian **1.14.2**, disposable `kplex-test`: privately compiled actual accepted `6b7b44a` GraphBuilder/property parser with the actual SDK against identical live inputs. All graph node/file/stat/neighbor fields and complete declaration multisets match with zero differences; discovery counts match. Baseline: **20,701 nodes/715,030 declarations**. Edited fixture cohort: **20,709/715,040**. No comparison graph was published; generated declaration IDs are excluded and multiplicity preserved.
- Real temporary notes tested Date targets, frontmatter array note type versus inline fallback, aliases/tags, two visual-only image occurrences versus a third prose occurrence, shared-URL referrer/origin lifetime, and retained malformed URL without origin. The edit used **one patch and zero full rebuilds**. Its declarations then equaled a clean rebuild and the accepted compiler's output.
- Final production artifact excludes the temporary SDK accessor. A fresh full compilation restored **20,701 nodes, 1,225,746 directed neighbor entries and 715,030 declarations**. Complete declaration digest matches accepted same-input baseline: `587e33e9a4eba5e6804f7348c39aa293dbe5ca0f7c8c1c4330520569be85bb57` (canonical sorted declaration JSON, generated IDs omitted, no separators). Native command/render/error smoke passed. All temporary notes/controllers/wrappers were removed; index ready, hydration settled, original background throttling true, no captured JavaScript errors. Source/final bundle contain no review accessor/global.

Final `main.js`: `45226b0b2855d9fcdd0a0b74c3de5fd676a0f8d55cfae83cb4fb52f4c500a5ab`. Manifest and CSS hashes are unchanged and recorded in JSON.

## Performance observations and limits

Final exact production foreground warm build: **9.828 s**, zero body reads/cached reads/parser calls and **79** body-cache lookups. Heartbeat max/p95: **88.4/33.4 ms**, zero hidden samples. Previous C12b warm observation was 8.333 s: the latest sample is about 18% slower. A pre-final empty-key compatibility correction run took 7.982 s; the latest final-artifact result is authoritative. These single observations do not establish statistical performance parity. Sampled heap before/after ~2,599/2,984 MB follows temporary private graph comparisons; it is neither peak memory nor causal regression evidence. C00 cold/physical-device/performance coverage remains open.

The initial accepted-baseline attempt timed out while hidden/background-throttled and is excluded. Semantic diagnostic runs temporarily disabled background throttling while preserving/restoring the prior true setting; their timings are excluded. Final production measurements used normal throttling. The first malformed native URL fixture was rejected by accepted parsing grammar; it was replaced with retained-invalid `https://%` before successful assertions. No failed probe is credited as a pass.

The 256-record cap bounds count, not bytes. Family/source/file revision fences do not establish an atomic whole-vault snapshot. Host acquisition, semantic fingerprints, TFile binding, parser/cache services and cooperative policy remain named compatibility seams. C13 makes compilation/parser portable; C14/C15 own incremental publication and revision coordination; C16 owns cache/storage orchestration. No collector retains persistent resources or a duplicate whole-vault input collection.

## Manual checks and next handoff

**No manual test required for C12c.** Complete same-input graph comparisons and actual native metadata edits cover this collection-only extraction. Interaction and platform routing are unchanged; no new physical-device result is claimed.

C13 is divided into C13a parser/runtime, C13b evidence/resolver and C13c full compiler. Parent C13 stays Pending until all three are accepted. **C13a is a Strong offline-agent candidate**; [HANDOFF.md](../../HANDOFF.md) is overwritten for that single slice. The separate measured worker-pool experiment does not belong in C13a. Rollback removes this collector, wiring, narrow contract extensions and tests; no user-data migration is required.
