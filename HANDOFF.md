# C12c handoff — remaining source facts and complete collector wiring

## Assignment and workflow

Implement **C12c only**, from the accepted C12b commit on `kplex-refactor`. Record the actual starting commit/dirty state, or unavailable Git metadata for a ZIP. This is a **Scoped offline-agent candidate**. Read `AGENTS.md`, `Refactor plan.md` sections 3–5/C11–C16, `docs/ARCHITECTURE.md`, `docs/NORMALIZED_SOURCE_CONTRACT.md`, `docs/INDEXING_ARCHITECTURE.md`, `docs/OBSIDIAN_RUNTIME_TESTING.md`, and C12a/C12b validation reports.

This single `HANDOFF.md` is transient and overwritten. Return uncommitted modifications, record actual results below, and mark C12c **Review**, not Done. The main agent independently reviews/fixes, runs mandatory portable and native checks and requests only necessary prioritized manual checks. The refactor plan retains accepted history. Parent C12 can close only when all existing source families actually cross the boundary; a partial named slice cannot close C12c/C12.

## Outcome

Finish production source collection for **Date properties, body URLs/origin inputs, presentation-only image link inputs, graph-relevant metadata and field-name discovery**, using C11 records and explicit Obsidian/runtime services. Keep GraphBuilder and the existing resolver as the sole semantic owners. This is an extraction, not a parser/compiler/scheduler redesign or feature request.

Work in buildable steps: **C12c1** Date facts; **C12c2** body URL/origin inputs; **C12c3** presentation link and semantic metadata/field-name facts; **C12c4** production wiring/residual-read inventory. Run unchanged characterization after each. Report exact completed and remaining slices if the whole assignment cannot safely be finished.

## Practical checkpoints

- [ ] **Inventory before edits.** Trace full/cooperative incremental paths, `addDatePropertyEvidence`, body URL loops, `suppressPresentationOnlyImageLinks`, `recordDiscoveredFields`/cooperative variant, note type/primary/style-tag handling, semantic fingerprints, parser/cache acquisition and file binding. List every host read, plain fact and compiler decision; identify existing overlaps to avoid duplicate families. C12a owns full structural tags; incremental parsed-tag contribution must preserve its distinct current semantics.
- [ ] **Date facts.** Move property registry/type checks, Moment parsing/formatting, Daily Notes enablement/folder/format and host destination resolution into the Obsidian adapter. Emit original property definition/name/raw value and exact normalized target identity, with materialized/unresolved state. Preserve configured hierarchy roles, case, invalid/array/missing values, fallback paths and exclusions in their existing owners. Do not invent Moment imports/APIs: inspect installed declarations and existing runtime wrappers. The compiler applies roles/evidence and target materialization, not host date parsing.
- [ ] **Body URL facts.** Consume already cached parser output, retaining raw URL, label and physical line, one shared target identity and separate declaring-note occurrences. Preserve malformed URLs as nodes with optional absent origin. Emit a valid origin target only when parsing succeeds; the compiler alone derives the existing origin→URL evidence and deduplication/lifetime. Do not add new Markdown URL indexing, canonicalization or shared-URL feature behavior.
- [ ] **Presentation link inputs.** Collect configured visual field/surface/reference counts and exact host target identities alongside existing aggregate link counts. Reuse the accepted property grammar/host resolution boundary. The compiler retains the exact rule that generic inferred evidence disappears only when every occurrence is accounted for solely by visual fields; mixed prose/ontology links remain. Preserve configured duplicates, frontmatter/inline overlap, target exclusions, attachment/Markdown distinctions and count multiplicity. No image bytes, decoded imagery or thumbnail values enter graph snapshots.
- [ ] **Semantic metadata and discovery.** Emit only existing aliases, raw tags, note-type/primary-tag inputs and property names/surfaces needed by current graph behavior. Keep competing frontmatter/inline values and configured identities distinct; compiler owns precedence, unwrapping, style selection and hierarchy. Preserve source-owned contribution replacement and discovery's rebuild-count versus patch-name behavior; arbitrary frontmatter values must not enter core/persistence. If the current contract lacks a necessary factual shape, make a narrow typed extension and justify it with parity tests rather than forcing facts into strings or adding semantic decisions to adapters.
- [ ] **Parser/cache/runtime ownership.** Keep cached reads, IndexedDB batches, parser work, field-cache and semantic fingerprints at their present ownership/budgets. C13 later makes the compiler/parser path portable; C14 handles private incremental commit; C15 coordinates demand/revisions; C16 handles persistence. Use explicit services for remaining host acquisition rather than declaring direct Vault/MetadataCache/Moment reads to be portable. Document each retained binding/compatibility facade, its exact callers and retirement checkpoint. Metadata acquisition may remain host-bound; semantic source facts must feed production compilation across the boundary.
- [ ] **Shared full/incremental facts.** Both full and cooperative patch compilation must use the same source normalization. Keep existing private state, affected-page handling, per-file fingerprints, semantic no-op policy, dirty backlog/follow-up and cleanup behavior. Do not rewrite deletion ghosts, tag hierarchy pruning, optimistic creation or incremental URL lifetime policy in this extraction. C14/C15 retain the separately tracked create/delete/revision work.
- [ ] **Bounds and validity.** At most 256 records per batch; preserve byte/read budgets, source-event revision checks after awaits, generation/cancellation and referential completion. Dense single fields/files must span batches without a second full-vault record array. Share immutable per-value/per-source facts where safe. Family-specific read boundaries do not by themselves establish an atomic whole-vault snapshot. Reject stale/reentrant/rejected attempts; no new scheduler/listener/deferred-edge store or persisted contribution cache.
- [ ] **Preserve C12a/C12b semantics.** Actual Obsidian root label is `/`; repeated host tag declarations remain; hierarchy alone deduplicates. Host-link counts are exact although legacy compilation creates one generic declaration per pair. Configured ontology labels/multiple roles retain multiplicity; property values deduplicate targets only within each legacy value/occurrence. External Markdown targets preserve accepted whitespace; URI decoding, subpaths and source-relative lookup preserve the accepted extractor. Core must not infer paths/kinds/case from opaque IDs or add a basename resolver.
- [ ] **Prove parity.** Keep existing graph/evidence/scene golden and strict timing thresholds unchanged. Cover Date targets and physical provenance, malformed URLs, shared URLs with multiple declarers, origin inputs, visual-only versus mixed links, counts/duplicate values, metadata precedence, raw tag contribution ownership and discovery. Assert active/suppressed evidence/multiplicity, full versus patch equality, cancellation at awaited boundaries, dense source batches and no host objects in records. Use accepted implementations/independent expectations, not only two new helpers that can share a regression.
- [ ] **Mandatory checks.** Required Node **22.22.2**, clean `npm ci`, `npm run verify`, real installed Obsidian types/build/lint and `git diff --check`. Wire new tests into mandatory scripts; never relax golden/timing/lint rules. Accurately report unavailable/failing lanes; test-only fixture producers and full arrays are not production implementations.
- [ ] **Prepare native acceptance.** Main agent deploys the exact build to disposable `kplex-test`, waits for authoritative initialization and forces a fresh full build before hashing (snapshot restoration alone does not exercise new collectors). Reference paths are under `Synthetic-Scale-v2/Reference/`. Compare complete graph/declaration hashes and body read/cache/parser counts; test relevant real metadata edits followed by clean rebuild equality. Keep renderer foreground/focus, avoid timing overlap with hash probes and distinguish sampled heap/heartbeat from peak memory/paint. Physical C00 performance remains pending if not actually tested.

## Guardrails

No schema/version/settings/default/UI/command/CSS/device/search/URL canonicalization changes. No new worker pool, persisted shards, second classifier, global plugin discovery in production or broad cleanup. Console stays English; new user copy must be localized, but this extraction should add none.

Collectors own no persistent resources. The source-revision compatibility accessor remains a C15 seam; legacy GraphPage file binding and compiler portability remain named C13 work. Explicitly record what still prevents the full compiler from running host-free. C12 completion is source-boundary adoption, not a claim that C13 is complete.

## Result — fill in on delivery

- Starting branch/revision/dirty state or unavailable Git metadata:
- C12c1–C12c4 completed production paths; exact residual host reads/facades and owners:
- Date/URL/presentation/metadata/discovery fact mapping and compiler decisions preserved:
- Identity/provenance/multiplicity and full/incremental parity:
- Dense-source count/byte bounds, revision/cancellation/completeness and cleanup:
- Exact commands, Node/dependency versions, results, failures and unavailable lanes:
- Host/device evidence (normally unavailable offline; never claim passed):
- Persisted keys/schemas changed (expected none):
- Rollback scope and any retained compatibility seams:
- Up to three prioritized reviewer checks with expected outcomes:
- Status: C12c Review; parent C12 pending main-agent completeness/host acceptance. Next: C13, Strong offline-agent candidate only after C12 closes; main agent prepares a separate compiler extraction handoff.
