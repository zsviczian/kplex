# Temporary handoff: C03a portable action button

**Status:** Returned and reviewed; C03 awaits maintainer keyboard/touch validation. **Base implementation commit:** `65b6a55` on `kplex-refactor` (`df05216` added this handoff); start from the branch HEAD containing this file. **Checkpoint:** C03, scoped offline slice C03a. **Final reviewer:** the maintainer's main agent with Obsidian access.

This is the one transient handoff file. Overwrite it at the next handoff; do not archive it. `Refactor plan.md` is the definitive tracker. Read `AGENTS.md`, the plan's sections 3.4, 3.6, 3.7, 4.4 and C03, plus `docs/ARCHITECTURE.md` and `docs/LOCALIZATION.md`. Do not mark C03 Done or edit its plan status. Return a reviewable changed-file set or branch and fill in the report below.

## Goal and exact scope

Extract **one host-free native action-button primitive** in `src/ui/components/` and migrate **only** the adjacent **Navigate back** and **Navigate forward** buttons in `src/ui/App.tsx`. These are the matched pair: both are ordinary icon actions with a label, disabled state and click callback. Keep their position, icon names and 17px icon size, disabled predicates, history callbacks, CSS class and English accessible labels unchanged. The existing local `ToolButton` must remain for the other toolbar controls; do not migrate toggles, menus, graph controls or `ThoughtNode` fold buttons in this slice.

The new component must render a real `<button type="button">` with an accessible label, native `disabled`, one icon slot supplied as React content and an ordinary click callback. Accept only props needed by this pair; it does not need toggle/pressed, async/busy or menu semantics. The portable component must not import `obsidian`, `ObsidianIcon`, the plugin, settings, `window` globals or DOM helpers. Compose the existing `ObsidianIcon` in `App.tsx` and pass it into the icon slot; Obsidian remains the icon source. Preserve the existing `excalibrain-icon-button` styling hook and let `App.tsx` own navigation intent.

Use L00's catalog for both labels: keep `toolbar.navigateBack` and add one `toolbar.navigateForward` entry with the unchanged English text **Navigate forward** and translator context. Pass the translated strings into the primitive. Do not add an HTML `title`: the existing document-scoped `LongPressTooltip.ts` delegates from buttons inside `[data-kplex-tooltip-scope]` and uses the ARIA label. Do not add a second tooltip or long-press listener. These two actions have no displayed shortcut; do not invent one. Leave console diagnostics in English.

Create `docs/UI_COMPONENTS.md` describing this implemented primitive, the icon-slot boundary, who owns the delegated tooltip, and the remaining legacy `ToolButton`. Record the **proposed** minimal `--kplex-*` token mapping for its foreground, background/border, focus and disabled states, based on the existing toolbar selectors in `styles.css` and Obsidian theme variables. **Do not edit `styles.css` in C03a**: the main reviewer will apply and validate the host CSS/token seam against the real light/dark/community theme and pop-out. This is intentionally a scoped handoff, not the whole C03 exit.

## Offline acceptance

- Add a small DOM-capable behavior lane invoked by `npm test` and `npm run verify`; choose the lightest suitable harness, update the lockfile if adding a dev dependency, and document the choice in `docs/UI_COMPONENTS.md`. Test the rendered primitive rather than matching source text: the action fires once for an enabled click, disabled never fires, the label and icon slot render, `type="button"` prevents accidental form submission, and the output has no `title`/second tooltip. Cover native keyboard activation if the harness supports it; leave physical keyboard/touch evidence to the reviewer.
- Run `npm run check:architecture`; the new component and all reachable imports must remain in the portable `components` layer. Keep the graph/index fixture suite separate from the DOM harness; do not attach React UI tests to its fake `window`.
- On Node **22.22.2**, run `npm run verify` (architecture, official Obsidian ESLint, aggregate tests and actual TypeScript/production build) and `git diff --check`. Report exact pass/fail, warning counts and any unavailable dependencies. Do not weaken architecture/lint/tests, and do not claim a stubbed render proves the live plugin.
- Preserve stable command IDs, navigation history behavior, persisted settings/data, view routing and all other controls. No graph/index changes, icon-library changes, broad restyling, general design system or new native shell code.

## Reviewer-owned C03 completion

The main agent will review the full returned diff, reproduce the pinned build/tests, implement/review the smallest host CSS token mapping, and stage the exact build in `kplex-test` via `verify:obsidian`. It will check both navigation actions, enabled/disabled state, focus/tooltip behavior, main window and pop-out, then use desktop tablet/phone emulation for layout and request physical mobile long-press validation if this slice affects touch behavior. Desktop emulation is not physical touch proof. The reviewer alone updates C03's durable ledger/status and commits the accepted checkpoint after required validation.

## External agent report — fill in before return

- Result and branch/commit or uncommitted changed-file set; starting revision: Implemented C03a as an uncommitted changed-file set from the handoff-declared starting revision `65b6a55`. This archive contains no `.git` metadata, so no branch/commit was created and C03 plan status was not edited.
- Files changed and why; exact two controls migrated: Added `src/ui/components/ActionButton.tsx`; migrated only the adjacent **Navigate back** and **Navigate forward** top-bar controls in `src/ui/App.tsx`; added `toolbar.navigateForward` in `src/lang/en.ts`; added/updated focused tests, `docs/UI_COMPONENTS.md`, and the `npm test` aggregate entry. `styles.css` is unchanged. The existing local `ToolButton` remains for all other controls.
- Primitive API and host-free import-boundary result (roots/reachable/violations): `ActionButton({ label, icon, disabled?, onClick })` renders `<button type="button" className="excalibrain-icon-button" aria-label={label} disabled={disabled}>…</button>`. The icon is a caller-supplied React slot. With the repository architecture checker run against a temporary link to the available global TypeScript installation: **10 migrated roots / 10 reachable files / 0 violations**; architecture checker self-tests **6/6 passed**. The component imports only the React `ReactNode` type and has no Obsidian/plugin/settings/global/DOM-helper dependency.
- Catalog keys, existing English/ARIA/icon/disabled/click behavior preserved: `toolbar.navigateBack` remains unchanged; added `toolbar.navigateForward` with exact English **Navigate forward** and translator context. `App.tsx` passes those translations as ARIA labels, keeps `arrow-big-left` / `arrow-big-right`, size `17`, the original history callbacks, and the original disabled predicates. No shortcut was added.
- DOM harness and behavioral cases; how tooltip ownership is kept single: Added a separate browser-backed DOM lane in `tests/ui-components.test.mjs`, invoked after the existing non-host suites by `npm test`. It bundles the real component with the repository's existing React/React DOM/esbuild dependencies and renders it in headless Chrome/Chromium (discoverable automatically or via `KPLEX_TEST_BROWSER`). It checks label and icon-slot rendering, exact CSS hook, enabled click fires once, disabled click never fires, `type="button"` does not submit a containing form, and no `title` is emitted. The dump-DOM runner does not inject trusted Enter/Space events, so physical/native keyboard activation remains reviewer evidence. `ActionButton` adds no tooltip/listener; existing document-scoped `LongPressTooltip.ts` remains the sole owner and reads the ARIA label under `[data-kplex-tooltip-scope]`.
- Proposed host CSS token map and any visual uncertainty: Documented, but did not apply, proposed `--kplex-action-fg -> var(--text-normal)`, `--kplex-action-bg -> var(--interactive-normal)`, `--kplex-action-bg-hover -> var(--interactive-hover)`, `--kplex-action-border -> var(--background-modifier-border)`, `--kplex-action-focus -> var(--background-modifier-border-focus, var(--interactive-accent))`, plus disabled foreground/opacity options (`var(--text-faint)`, current `.28`). Reviewer must validate the smallest mapping against current toolbar appearance, focus visibility, light/dark/community themes, owning-document scope and pop-outs.
- Node/npm versions; commands, exact pass/fail/warning results; build and diff check: Available runtime is **Node v22.16.0 / npm 10.9.2**, not the required Node 22.22.2; `/opt/nvm` contains only 22.16.0. Registry access failed with `EAI_AGAIN`, so repository dependencies could not be installed. `npm run verify` therefore **failed immediately** in `check:architecture` because project-local `typescript` was unavailable; Obsidian ESLint, production TypeScript/build, and the browser DOM lane could not be executed, so their warning counts are **not measured**. Using a temporary symlink to the globally available TypeScript only, `node scripts/check-architecture.mjs` **passed: 10 roots / 10 reachable / 0 violations** and `node --test tests/architecture.test.mjs` **passed 6/6**. The presentation/localization/Obsidian-runner/large-vault-generator aggregate **passed 28/28**. `node scripts/run-indexing-tests.mjs` **failed** an existing timing-sensitive responsiveness assertion (`Inline-field cooperative parser blocked timers for 132.6 ms`) on Node 22.16.0; no graph/index code was changed. `node --check tests/ui-components.test.mjs` passed. A real `git diff --check` is unavailable because the supplied archive has no `.git`; `git diff --no-index --check` between the pristine upload and changed tree emitted **no whitespace-error output** (exit 1 solely because files differ). No new npm dependency was added, so `package-lock.json` is unchanged.
- Obsidian, pop-out, theme and physical-touch checks unavailable to you: Not run. No installed/running Obsidian test-vault lane was available in this external environment; main-window/pop-out appearance, live focus/keyboard behavior, light/dark/community themes, and physical long-press/touch remain reviewer-owned exactly as specified by the handoff.
- Remaining legacy `ToolButton` callers and who owns their later migration: Four callers remain in `App.tsx`: **Display aliases**, **Single-level/Expanded view**, **straight/curved connectors**, and **Open K-Plex settings**. They were intentionally not migrated. The main reviewer / later C03 work owns any subsequent migration after host CSS and live interaction validation.
- Highest-probability regression, prioritized reviewer checks and open questions: Highest risk is visual/focus-state drift once the proposed host token seam is actually applied, not the history logic: the production callbacks/predicates/icons remain in `App.tsx`. Reviewer P0: run pinned Node 22.22.2 `npm run verify`, including the new browser DOM lane, and confirm lint warning count/build. Reviewer P1: in exact staged Obsidian build, click back/forward through enabled and disabled states, verify 17 px icons and toolbar position, keyboard focus/activation and delegated tooltip in main window and pop-out. Reviewer P2: validate the eventual token mapping across light/dark/community themes and physical long-press click suppression if touch is affected. Open design choice is which minimal disabled/focus token mapping best preserves current appearance; C03a intentionally leaves that to host review.
