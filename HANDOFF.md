# Temporary handoff: E00 host environment seam

**Status:** Ready for external agent. **Implementation baseline:** `f7dfd35` on `kplex-refactor`; start from the branch HEAD that contains this handoff file.
**Owner of final acceptance:** the maintainer's reviewing agent with Obsidian access.
This file is a one-task exchange document. `Refactor plan.md` is the definitive checkpoint tracker; after review, the reviewing agent transfers accepted evidence there. The next handoff overwrites this file in place rather than archiving it. Do not turn this into a continuing log.

## Assignment

Implement **E00 — Establish the host environment seam without changing device behavior**, as specified in `Refactor plan.md` (section 5, E00) and `docs/ARCHITECTURE.md`. You do **not** need an installed or running Obsidian application. The repository's installed `obsidian` package supplies types for the real build. Work from the base commit above on a separate branch or as a reviewable working-tree diff; do not merge or publish.

Read `AGENTS.md` first. Before editing, inspect `src/ui/viewProfile.ts`, `src/settings.ts` layout-profile keys/migration, `src/main.ts` command registration and `activateView()`/`activateSidepanel()`/`activateViewInPopout()`, and current platform references in `src/ui/`. Preserve current routing and persisted settings. `mobile` is the existing **phone profile key**; do not rename it to `phone` in stored data. `Platform.isMobile` includes tablets; the current classifier prefers optional runtime `isPhone`/`isTablet` flags and otherwise uses the shortest screen side. Confirm any Obsidian API you use against the installed declarations rather than guessing.

Create a small **host-free presentation-environment contract** with separate device class (`desktop`/`tablet`/`phone`), OS/key convention, input modes (keyboard/pointer/touch may coexist), and only the host actions actually needed by the first consumer. Collect Obsidian facts at an Obsidian-owned boundary. Keep the existing classifier's behavior, including fallback and command visibility. No process-wide mutable platform singleton. Keep the environment out of graph semantics, indexing, cache and persistence; iOS-specific indexing/memory policy stays where it is. Use an explicit `phone` → persisted `mobile` mapping where layout profile keys are selected.

Migrate one **real, narrow presentation consumer** to accept the contract through an argument or prop rather than importing `Platform` or reading `window`. A pure layout-profile selector is a viable candidate if it remains the production path and keeps existing `leaf`/`sidepanel`/`popout` profile selection intact. Avoid inventing a demo component or moving all of `PlexGraph.tsx` merely to claim portability. Keep compatibility delegates where legacy callers still need them, and name their owner and retirement checkpoint in your report. Do not start L00 localization, C03 action components, shortcut-copy redesign, a full feature-availability framework, or the separate filtered gate-count fix ([issue #28](https://github.com/zsviczian/kplex/issues/28)).

## Acceptance checks you can run without Obsidian

- Pure tests cover desktop, tablet, phone; macOS, Windows, iOS/iPadOS, Android and unknown convention; touch-only, keyboard plus touch, and pointer plus keyboard; available/unavailable pop-out and graph-tab actions. Test the current classifier's optional flags and screen-size fallback, including phone/tablet disagreement cases, without creating an Obsidian runtime dependency in the pure tests.
- Verify unchanged phone → sidepanel, tablet → normal tab/sidepanel, desktop → normal tab/pop-out routing and command visibility, ideally with focused behavior assertions around extracted decision functions. Preserve stable command IDs and persisted layout-profile keys, values and migration. Do not bless changed defaults or rewrite baselines to match an accidental behavior change.
- The migrated portable consumer's **transitive** imports pass `npm run check:architecture`; report the migrated-root count (a zero-root pass does not prove the work). `npm run verify` passes on Node 22.22.2, including Obsidian ESLint, tests and the real production build. Run `git diff --check` and inspect the full diff for unwanted persisted/schema/UI-copy changes.
- If Obsidian CLI or a device is unavailable, record the applicable host checks as **pending**, not passed. Do not make `verify:obsidian` a requirement of the portable lane. Do not add production diagnostics or generated bundles to source control.

The reviewing agent will later run applicable Obsidian checks in the disposable `kplex-test` vault: desktop command visibility/open/pop-out and layout-profile selection; tablet normal-tab and sidepanel behavior; phone sidepanel routing and physical touch where a device is available. The large synthetic vault is not needed to prove this seam. If a platform cannot be exercised, its check remains pending in `Refactor plan.md`.

## Return instructions

Update **only the report below** with what you actually changed and observed. Leave the E00 row/status and action log in `Refactor plan.md` for the reviewing agent, who will validate your code and transfer accepted evidence. Do not claim E00 Done from offline checks alone. Stop at E00's boundary and leave L00 pending.

### External agent report

- Result/status (implemented, partial, or blocked): Implemented. The E00 source changes and portable verification are complete; final acceptance remains with the reviewing agent because the required Node 22.22.2/full dependency environment and Obsidian/device checks were not available here.
- Starting revision and final branch/commit or uncommitted diff: Started from the supplied archive whose handoff identifies baseline `f7dfd35` (`kplex-refactor`). The archive contains no `.git` metadata, so the result is an uncommitted reviewable diff rather than a new branch/commit.
- Files changed and reason for each: Added `src/core/contracts/presentationEnvironment.ts` (host-free presentation contract), `src/core/plex/viewPresentation.ts` (pure classifier/routing/profile decisions), `src/adapters/obsidian/presentationEnvironment.ts` (fresh Obsidian fact collection), and `tests/presentation-environment.test.mjs` (E00 behavior coverage). Updated `src/ui/viewProfile.ts` to be a compatibility facade over the pure selector, `src/main.ts` to consume the new seam for command visibility/open routing/profile selection, `tests/indexing.test.mjs` to compile/stub the new production dependency, `package.json` to include the focused test, `docs/ARCHITECTURE.md` to document the migrated seam/facade, and `AGENTS.md` to replace the now-stale pre-E00 platform guidance. This report is the only other edited content in `HANDOFF.md`.
- Existing behaviors and persisted keys preserved; evidence: `src/settings.ts` is byte-identical to the supplied baseline, including all `desktop:*`, `tablet:*`, and `mobile:*` layout-profile keys and the existing migration loop. The pure profile selector explicitly maps presentation `phone` to persisted `mobile`; no `phone:*` stored key was introduced. Stable command IDs (`excalibrain-start`, `kplex-open-popout`, `kplex-open-sidepanel`) remain unchanged. Focused tests assert phone -> sidepanel with graph-tab/pop-out commands unavailable, tablet -> normal tab with graph-tab available and pop-out unavailable, and desktop -> normal tab or pop-out according to the existing setting. `activateView()` still reuses an existing graph leaf before creating a new target. The existing `Platform.isMobile` sidepanel timing workaround and iOS indexing/memory policy were intentionally left outside the presentation seam. `Refactor plan.md` is byte-identical to baseline.
- Portable consumer migrated and exact reachable architecture-check result: Production layout-profile selection and open/command-routing decisions now flow through the host-free `src/core/plex/viewPresentation.ts` functions, with `main.ts` passing a freshly read `PresentationEnvironment`; `src/ui/viewProfile.ts` delegates profile selection to that pure path. `npm run check:architecture` passed with `Architecture: 3 migrated roots, 3 reachable files, 0 violations` (architecture checker self-tests also passed 5/5).
- Tests/commands, Node version, results and what they prove: Available runtime was Node `v22.16.0` / npm `10.9.2`, not the required Node 22.22.2. `node --test tests/presentation-environment.test.mjs` passed 8/8 (classifier flags/fallback/disagreement, OS/key conventions, mixed input-mode values, host-action availability, routing, persisted phone->mobile mapping, stable IDs/keys, portable-source checks). `node --test tests/presentation-environment.test.mjs tests/obsidian-runner.test.mjs tests/large-vault-generator.test.mjs` passed 15/15. `npm run check:architecture` passed as reported above. A targeted TypeScript compile of the new contract/policy/adapter passed using the available TypeScript 5.8.3 and a minimal declaration for the referenced Obsidian `Platform` fields; this is not a substitute for the real production build. An equivalent `git diff --no-index --check` over the changed/new files was clean because the supplied archive has no `.git`. `npm test` reaches the existing indexing responsiveness benchmark and fails on `Inline-field cooperative parser blocked timers` (~226 ms here); the untouched supplied baseline reproduces the same failure (~214 ms) on this Node/runtime, so no benchmark code was changed. Full `npm run verify` could not be completed: dependency installation is unavailable in this container (`npm ci --offline` reports an uncached package; online registry DNS is unavailable), leaving the real build without required type packages and Obsidian ESLint without `eslint`.
- Host/device checks not run and why: Obsidian CLI/test-vault access and physical tablet/phone devices are unavailable in this environment. Desktop open/pop-out/profile selection, tablet normal-tab/sidepanel behavior, phone sidepanel routing, and physical touch checks therefore remain pending for the reviewing agent as instructed.
- Highest-risk remaining behavior and prioritized test for the reviewing agent: Highest remaining risk is host-level form-factor/action behavior at the real Obsidian boundary (especially tablet versus phone classification and window/pop-out support). Prioritize exercising the three real form factors in `kplex-test`: verify command visibility, `activateView()` routing/reuse, and selected persisted profile key (`mobile:*` on phone) before broader regression testing.
- Any deviations, unresolved questions, or suspected regressions: No intentional UI copy, settings/schema, graph/index/cache/persistence, localization, C03, shortcut, or issue-#28 changes were made. The production input-mode adapter is deliberately conservative: browser/Obsidian facts do not reliably prove presence of a physical keyboard on a mobile device, so it exposes the contract's coexistence model without inventing mobile-keyboard presence; the migrated E00 consumer does not depend on `inputModes`. Exact verification against the repository's installed Obsidian package and Node 22.22.2 remains pending because those dependencies/runtime were unavailable after the failed install. Focused source/behavior checks found no suspected E00 regression.
- Remaining legacy callers, compatibility facade owner, and intended retirement checkpoint: `src/ui/viewProfile.ts` remains the UI/composition-owned compatibility facade for legacy profile helpers and its legacy persisted-device-class delegate (`currentDeviceClass()`); new production routing in `main.ts` no longer depends on that legacy classifier return shape. Keep the facade while remaining callers migrate, then retire it at C25 (`Complete enforcement and retire migration facades`), as documented in `docs/ARCHITECTURE.md`.
