# C12b handoff — host link summaries and ontology source facts

## Assignment and workflow

Implement **C12b only**, after accepted C12a. This is a **Scoped offline-agent candidate**: the main agent owns exact-build Obsidian acceptance. Read `AGENTS.md`, `Refactor plan.md` sections 3–5 and C11–C15, `docs/ARCHITECTURE.md`, `docs/NORMALIZED_SOURCE_CONTRACT.md`, `docs/INDEXING_ARCHITECTURE.md`, `docs/OBSIDIAN_RUNTIME_TESTING.md`, and C11/C12a validation reports. Expected branch: `kplex-refactor`; record actual starting commit/dirty state, or explicitly unavailable Git metadata for a ZIP.

`HANDOFF.md` is one transient assignment, overwritten rather than archived. The definitive tracker is the refactor plan. Return uncommitted changes and mark C12b **Review**, not Done. The main agent reviews/fixes, reproduces portable checks, performs host tests and requests any necessary prioritized manual tests before committing acceptance. Parent C12 remains incomplete until C12c passes too.

## Outcome and scope

Move **resolved/unresolved host link summaries and frontmatter/inline ontology input collection** behind explicit Obsidian adapter modules. The production GraphBuilder must consume C11 source facts while continuing to own graph pages, evidence, role classification, directions, exclusions, precedence and inverse views. Keep the existing compiler/resolver authoritative; adapters supply facts and exact host-selected target identities, not classified relationship decisions.

Implement in two buildable steps: **C12b1** host link-map summaries, then **C12b2** ontology occurrences/target resolution. Run the unchanged graph/evidence/scene characterization after each. If both cannot be delivered safely, explicitly report the completed named slice and remaining work; never mark parent C12b complete after only link maps.

Date/Daily Notes, body URLs/origins, image-only reconciliation and final collector wiring remain C12c. Parser/cache services and portable grammar/compiler extraction remain C13. Incremental staging/publication is C14; demand/revision coordination is C15; persistence/search are later. Do not rewrite scheduler or compiler algorithms to accommodate an extraction.

## Practical checkpoints

- [ ] **Inventory reads and consumers.** Trace full-build `addResolvedLinks()`/`addUnresolvedLinks()`, full and incremental ontology loops, `extractLinksFromValue()`, metadata merge/inline occurrence outputs, semantic fingerprints and image-only exclusions. List exact remaining host reads and retirement owners. C12a now owns full structural reads; do not reintroduce them or disturb its validity fence.
- [ ] **Migrate link-map summaries first.** Collect one source/target summary with exact host count, raw map target identity and explicit resolved/unresolved state. Consume records using current evidence helpers. Preserve original skip rules, configured index-source/target exclusions, case/path lookup behavior and missing-source/target handling. A host summary does not supply a literal link spelling/location/heading: do not fabricate those. Image-only reconciliation later needs the same host counts.
- [ ] **Migrate ontology facts second.** Keep Markdown acquisition/parser-cache ownership intact; extract only configured-field occurrence collection and host destination resolution into an adapter. Preserve original configured frontmatter definition/field label, actual inline field label, raw value, physical line/start/end where owned, source-relative resolution and shared target identity. Preserve every conflicting, repeated, mirrored and hidden occurrence. The compiler, not the adapter, applies hierarchy roles and property-over-body precedence. Multiple configured role groups must not accidentally disappear through field deduplication.
- [ ] **Share fact normalization across applicable paths.** Inventory full/cooperative incremental ontology consumers and avoid a parallel normalizer. Preserve their existing scheduling/commit ownership; do not port incremental publication early. If one path must temporarily stay legacy, record its exact consumer list, why, and named retirement checkpoint. Remaining image-reconciliation uses of the legacy link extractor stay until C12c; grammar portability moves in C13.
- [ ] **Keep identity and host binding explicit.** Reuse opaque NodeId and independent paths/kind/facets. Host resolution stays adapter-owned (source-relative ambiguous names, headings, URL or unresolved targets). Never derive a path/kind from an ID or bundle a new global basename resolver. Preserve the actual legacy target selected by `getFirstLinkpathDest`/existing extractor and GraphBuilder materialization, including index/self exclusions applied by the compiler.
- [ ] **Preserve source multiplicity.** Retain host link summaries alongside ontology declarations for the same pair, conflicting frontmatter/inline roles, repeated values and raw source provenance. Do not add source-declaration deduplication because final topology looks unchanged. C12a native review caught that error for repeated host tags; the golden graph alone is not enough unless evidence multiplicity is checked too. Hierarchy deduplication and semantic precedence retain their existing owners.
- [ ] **Bound the stream and keep timing cooperative.** At most 256 records per batch, dense fields spanning multiple batches, no second whole-vault record/metadata array. Preserve existing read/cache/parser budgets and yields; check cancellation/current source revision after awaits. Reuse immutable raw field values rather than serializing/copying a huge configured field once per target. No eager arbitrary frontmatter or imagery crosses core. Tests must cover one dense source, not only many small sources.
- [ ] **Make validity and completeness honest.** Reuse C12a's existing source-event revision capability (`getIndexSourceRevision()` currently delegates to the plugin dirty revision), distinct from mtime/generation. Preserve current dirty-backlog/follow-up ownership. Define scope of any family batch boundary; do not falsely call independent family attempts an atomic whole-vault snapshot. Reject mixed/stale/reentrant/rejected streams and retain pending source work. Materialized endpoints already collected structurally and virtual targets created by existing compiler helpers must remain complete; no unbounded deferred-edge store. C14/C15 own final latest-wins publication and create/delete races.
- [ ] **Prove exact behavior.** Keep the existing compatibility graph/evidence/scene golden unchanged. Assert declaring-source/target multisets, raw definitions/values/locations, active/suppressed decisions and occurrence multiplicity. Include source-relative same basenames, subpaths, case-distinct paths, unresolved references, ordinary+ontology overlap, conflicting fields, self/index exclusions, hidden declarations, shared URLs with distinct declarers, and visual-field links that must remain available to C12c reconciliation. Target path should preserve legacy alias/subpath semantics rather than the fixture producer's simplified rules.
- [ ] **Mandatory verification.** Node **22.22.2**, clean `npm ci`, `npm run verify`, `git diff --check`. Wire meaningful tests into mandatory scripts; use installed real Obsidian types for production build. No relaxing performance thresholds, golden assertions or lint rules to make delivery pass. Report unavailable install/Git/host lanes accurately. Test fixture parser/hash IDs/full arrays are not reusable production implementations.
- [ ] **Prepare main-agent host acceptance.** Identify changed production paths, expected body read/cache/parser counts, revision/cancellation checks, cleanup owner and rollback. Main agent will deploy the exact build to `kplex-test`, wait for full authoritative readiness, and compare against the same source cohort and accepted behavior. The test vault's reference notes live under `Synthetic-Scale-v2/Reference/`, not the portable fixture's root paths. Renderer focus/initialization and extra graph probes can distort timings; keep unavailable/native results pending rather than claiming an offline pass.

## Guardrails

No schema/version/settings/default/URL canonicalization/search ranking/UI/command/CSS/layout/device policy change. No parser-worker pool, persisted semantic contributions, new persistence layer, second classifier or global plugin escape hatch. Console diagnostics stay English; user copy uses localization if genuinely needed, but this extraction should add none.

C12a retains raw duplicate tag memberships and resolves each raw hierarchy once through a run-local graph-page cache. Real root path `/` with host name `""` maps to semantic root label `/`. Do not change these accepted semantics. The source revision compatibility accessor moves with C15 coordination; legacy GraphPage.file binding stays at the adapter boundary until later graph migration.

## Result — fill in on delivery

- Actual branch/starting revision/dirty state, or unavailable Git metadata:
- C12b1/C12b2 production consumers migrated; exact residual reads and retirement checkpoints:
- Identity/resolution/provenance/multiplicity mapping; compiler owners preserved:
- Batch/byte bounds, revision fences, endpoint completion and cancellation behavior:
- Compatibility golden, evidence/decision comparisons and dense-source tests:
- Node/dependency versions; exact commands/results and unavailable/failing lanes:
- Host/physical-device evidence (normally unavailable offline; never claim passed):
- Persisted keys/schemas changed (expected none):
- Cleanup owner, rollback scope and retained facades:
- Up to three prioritized reviewer checks with exact expected outcomes:
- Status: C12b Review; parent C12 remains incomplete. Next after full C12b acceptance: C12c, Scoped offline-agent candidate for Date/URL/body/presentation facts and collector wiring.
