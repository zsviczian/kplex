# Temporary handoff: L00 English localization foundation

**Status:** Returned and reviewed; L00 awaits the physical F4 key check recorded in `Refactor plan.md`. **Base implementation commit:** `b65632a` on `kplex-refactor`. **Final reviewer:** the maintainer's main agent with Obsidian access.

This is the single transient handoff file. Overwrite it for the next assignment; do not archive it or use it as the progress ledger. `Refactor plan.md` is definitive. Read `AGENTS.md`, the plan's sections **3.6**, **3.7**, **4.4**, **5/L00** and `docs/ARCHITECTURE.md` before editing. Work only on L00 and return a reviewable diff or branch; do not merge, publish, or mark L00 Done in the plan.

## Objective and boundaries

Create one English source catalog under `src/lang/` and a typed lookup/formatting API that supports named parameters, English plural forms and an English fallback when a future locale has no translation. Unknown keys, missing interpolation values and malformed plural forms must fail in focused tests; avoid blank/raw-key UI output. The catalog keys and examples should be clear enough for later translators. **Do not create translations in this checkpoint.** The Obsidian boundary obtains the configured language through the installed typed `getLanguage()` export in `node_modules/obsidian/obsidian.d.ts`; portable code receives a narrow translator/capability instead of importing Obsidian or reading `window.app`/local storage.

Migrate a **small, representative, production-used set** of existing user-facing strings while preserving their current English wording and command IDs: one React control/ARIA label, one native command or shell label, one user-visible `Notice`, and one help/tooltip/ARIA surface. `src/ui/App.tsx`'s topbar controls, `src/main.ts`'s command registration/notices, and `src/ui/NewRelatedNoteModal.ts` are starting points, not a mandate to rewrite whole files. If you touch `NewRelatedNoteModal.ts`, keep its compatibility `.tsx` copy synchronized as required by `AGENTS.md`; esbuild deliberately prefers `.ts`. Do not translate vault titles/content, ontology field names, stable IDs, persisted keys, machine-readable `aria-keyshortcuts` tokens or English developer console diagnostics. L01 will migrate the remaining legacy strings and enforce whole-source completeness; record an inventory rather than sweeping them now.

Add a **pure shortcut formatter** based on E00's `PresentationEnvironment` and one representative displayed hint passed to catalog copy as a **named parameter**. It must describe an action the current handler/registration actually accepts. Good candidates to inspect are `src/ui/App.tsx`'s F4 / Ctrl-or-Meta+F search handler or `src/ui/NewRelatedNoteModal.ts`'s `Mod+Enter` action. Keep `aria-keyshortcuts` in its standard machine syntax. Show Command/Option on macOS and Control/Alt on Windows as applicable; handle iOS/iPadOS, Android and unknown conventions explicitly. Do not advertise a fixed host-configurable binding if it cannot be read, and do not show a keyboard hint when the action is unavailable or the only known input is touch. E00 reports mobile keyboard presence as `unknown`: do not equate that with confirmed absence or invent attached-keyboard detection. No shortcut behavior change is authorized just to make a label easier to format.

Keep graph semantics, indexing, cache, persisted formats, device routing and C03 UI-component extraction out of scope. Avoid a generic localization framework, runtime network loading or large bundles. A catalog alone does not complete L00: the representative production consumers and tests must use it.

## Offline implementation and verification

- Inspect current callers and note the exact pre-change English strings, handler/registration and required capabilities. Use the installed Obsidian typings for `getLanguage()`; do not guess host APIs. Identify the chosen representative surfaces in your report.
- Focused tests must prove catalog key/parameter/plural validation, English fallback, deterministic shortcut output for macOS/Windows and relevant mobile/unknown cases, omission for unavailable/touch-only actions, and the match between the displayed hint and the real logical action. Test a representative React/host consumer through a suitable harness rather than only asserting source text. Ensure new test files are invoked by `npm test` and `npm run verify`.
- `npm run check:architecture` must report the reachable migrated roots and zero violations. Portable localization/shortcut code must not transitively import Obsidian, DOM/window globals, settings or the plugin. Run `npm run verify` on Node **22.22.2** (architecture, Obsidian ESLint, tests and actual production build), plus `git diff --check`. Report the exact result, including any pre-existing warning count; do not weaken tests or suppress lint to finish.
- Document the implemented catalog key/parameter/plural conventions and how the host supplies language and shortcut facts for future feature agents. Update `AGENTS.md` only to describe rules that the code now enforces or directly supports; do not claim L01's whole-source hard-coded-copy gate exists yet.
- No Obsidian installation is needed for your implementation. If unavailable, mark desktop text/ARIA/notice/hint checks as **pending**. Do not claim a stubbed render or compiled catalog is live-host proof, and do not make the portable lane depend on `verify:obsidian`.

The main agent will review the full diff, fix issues, run the exact build in the disposable `kplex-test` vault through `verify:obsidian`, inspect the chosen command/control/notice/ARIA text and shortcut hint, then request only any high-value manual test automation cannot cover. The reviewer will write the durable L00 ledger/action record in `Refactor plan.md`. Do not commit generated `dist/`, a test vault, or temporary reports.

## External agent report — fill in before return

- Result (implemented/partial/blocked), starting revision and final branch/commit or uncommitted diff: **Implemented; pinned-toolchain/full-host verification is partially blocked by this container.** The requested starting revision is `b65632a` on `kplex-refactor` per this handoff. The supplied repository archive contains no `.git` metadata, so I could not independently resolve that revision, create a branch/commit, or run the repository form of `git diff --check`; the return is an uncommitted changed-file set relative to the supplied archive. `Refactor plan.md` was not edited and L00 remains Pending for reviewer acceptance.
- Files changed and why; catalog keys and exact representative consumers migrated: Added `src/lang/en.ts` (English source catalog) and `src/lang/index.ts` (strict typed translator/formatter); added `src/adapters/obsidian/localization.ts` (`getLanguage()` host boundary); added `src/core/plex/shortcutPresentation.ts` (host-free shortcut spec/formatter/handler predicate) and `src/ui/features/searchPresentation.ts` (portable search copy composition). `src/main.ts` now uses `command.openGraph` for command id `excalibrain-start`, `notice.excaliBrainSettingsImported`, and plural-capable `notice.indexedNodes`. `src/ui/App.tsx` uses `toolbar.navigateBack` and search copy; `src/ui/SearchBox.tsx` receives `search.placeholder` / `search.placeholderWithShortcut` and `search.ariaLabel`; `src/ui/ExcaliBrainView.tsx` supplies the translator and fresh presentation environment. The complete L00 catalog keys are `command.openGraph`, `notice.excaliBrainSettingsImported`, `notice.indexedNodes`, `search.ariaLabel`, `search.placeholder`, `search.placeholderWithShortcut`, and `toolbar.navigateBack`. Tests, architecture rules/docs and `AGENTS.md` were updated only for the implemented L00 contract; `package.json` now includes the focused localization tests in `npm test`.
- Existing English copy, command IDs, shortcut behavior and persisted data preserved; evidence: Migrated literals preserve **Open graph**, **Imported ExcaliBrain settings into K-Plex.**, **K-Plex indexed {count} nodes.**, **Navigate back**, **Search nodes…**, and **Search nodes**. The required visible shortcut hint extends the existing search placeholder only when a confirmed keyboard/convention supports it. Command id `excalibrain-start` is unchanged and is asserted by the focused tests; the existing presentation test also passes its stable command-id/persisted-layout-key assertions. Search still accepts exactly the prior logical gestures: `F4`, or `F` with Ctrl or Meta and without Alt; that predicate was extracted rather than changed. No settings schema, persisted keys/formats, graph/index semantics, ontology data, vault content or device-routing policy were changed. All existing aggregate indexing/presentation/runner/generator tests pass.
- Language source/fallback behavior and portable import-boundary result (root/reachable counts): English is the only shipped catalog. Catalog entries require translator context, declared named parameters, and exactly one message or English `one`/`other` plural pair; unknown keys, undeclared/missing params, malformed/blank entries and nonnumeric plural counts throw instead of leaking raw keys/blank UI. Host locale identifiers normalize `_`/`-` and case, then resolve exact locale → base locale → English; tests inject synthetic locale data only and no translation file was added. Obsidian language acquisition is isolated to `src/adapters/obsidian/localization.ts`; portable localization/shortcut/search-presentation modules do not import Obsidian, plugin/settings code, DOM/window globals or storage. The architecture checker reports **9 migrated roots, 9 reachable files, 0 violations**.
- Shortcut specification/handler chosen, displayed hint rules and tests by environment/input mode: `SEARCH_FOCUS_SHORTCUT` is logical `Mod+F`, matching one branch of the existing local search handler; `F4` remains accepted but is not advertised as the representative hint. Formatting renders `Command+F` on macOS/iOS and `Control+F` on Windows/Android when keyboard presence is explicitly `true`; unknown key convention, unavailable action, keyboard `false`, touch-only input, and mobile keyboard state `"unknown"` omit the hint and fall back to `Search nodes…`. The complete formatted shortcut is passed to `search.placeholderWithShortcut` as named parameter `{shortcut}`. Focused tests cover macOS, Windows, iOS/iPadOS convention, Android, unknown convention, unavailable/touch-only/unknown-keyboard cases, Ctrl/Meta/F4 handler acceptance and Alt rejection, plus an executable `SearchBox` React-consumer harness proving the localized placeholder/ARIA values reach its input surface.
- Node/npm versions, commands run, pass/fail results and what each proves: Container versions are **Node v22.16.0 / npm 10.9.2**, not the required Node 22.22.2. `npm test` **PASS**: indexing assertions 1–68 plus placeholder/ghost fixture pass and Node test runner reports **26/26** passing, including **10** L00 localization/shortcut/React/host-adapter tests. `node --test tests/architecture.test.mjs` **PASS 6/6** and `node scripts/check-architecture.mjs` **PASS: 9 roots / 9 reachable / 0 violations**. A clean host-free TypeScript check using the container's global TypeScript over the new lang/shortcut/search-presentation modules **PASS**. Per-file `git diff --no-index --check` over the supplied original vs changed/new files **PASS** as a whitespace substitute; the archive lacks Git metadata so repository `git diff --check` itself is unavailable. `npm run verify` was attempted and architecture passed, then it **FAILED at `lint:obsidian` because `eslint` is not installed**. Dependency installation cannot be repaired in this container: npm registry access returns `EAI_AGAIN getaddrinfo registry.npmjs.org` (and `npm ci` could not complete), so official Obsidian ESLint and the actual dependency-backed production `tsc`/esbuild build could not run. The plan/handoff records the pre-existing expected lint baseline as **25 sentence-case warnings**, but that count was **not re-observed here** and must be reconfirmed on Node 22.22.2 with dependencies present.
- Obsidian/device checks unavailable and the precise host workflow for reviewer validation: No live Obsidian/test vault was available here, so live desktop text/ARIA/notice/hint checks remain **pending**. Reviewer: (1) on Node **22.22.2**, run `npm ci`, `npm run verify`, and confirm zero errors plus the unchanged expected warning baseline; run repository `git diff --check`; (2) run `npm run verify:obsidian` against the disposable `kplex-test` vault and confirm staged/built hashes, rendered K-Plex and no captured JS errors; (3) on desktop, open K-Plex and verify **Navigate back** is unchanged, search shows `Search nodes… (Command+F)` on macOS or `Search nodes… (Control+F)` on Windows, its accessible label remains **Search nodes**, and both the displayed Mod+F gesture and F4 focus search; (4) confirm command-palette **Open graph** still invokes command id `excalibrain-start`; rebuild the index with notice enabled and confirm **K-Plex indexed N nodes.** appears unchanged. If checking a phone/tablet, confirm the search placeholder has no keyboard hint when E00 reports keyboard `unknown`; do not infer attached-keyboard state from that omission.
- Remaining hard-coded user-copy/key-hint inventory for L01, by surface: **Host shell/main:** remaining command labels (for example Rebuild index, pop-out/sidepanel, Search, sync/focus actions), rebuild/web-view/error notices, unfolded-view ARIA and other native menu/modal labels. **Top-level React:** remaining App topbar/tooltips/ARIA such as Navigate forward, alias/depth/connector/settings controls, sync/pinned-node labels; `FuzzySearchInput` generic default `Search…`/`Search`; `PlexFilter`, `PlexGraph`, `ThoughtNode` and relation popover labels/tooltips/ARIA. **Native modal flows:** `NewRelatedNoteModal.ts` and synchronized `.tsx`, CreateFolderNote, DeleteNode, MaterializeGhost, RenameNote, RelationModal, RelationshipExplanationModal, AddToOntology and NoteType copy/notices. **Settings:** the large legacy settings surface in `src/settings.ts` (names, descriptions, buttons/modal text). L01 should inventory these systematically, migrate them without changing English meaning, and add the planned negative whole-source hard-coded-copy gate; this L00 intentionally does not claim that gate exists.
- Highest-probability regression and 1–3 prioritized reviewer tests with expected outcomes: **Highest risk:** an integration/type/build issue around the newly imported typed Obsidian `getLanguage()` or React prop wiring that the dependency-less container cannot prove with the real production build. **P0:** Node 22.22.2 `npm run verify` and `verify:obsidian`; expected zero errors, unchanged warning baseline, successful production build/render, no captured JS errors. **P1:** desktop search/back control; expected unchanged **Navigate back** and **Search nodes** ARIA, platform-formatted Mod+F placeholder, and both Mod+F/F4 focus search with no shortcut semantics change. **P2:** host command/notice; expected **Open graph** remains available under the same stable command id/routing policy and a rebuild displays the unchanged `K-Plex indexed N nodes.` notice.
- Open questions, deviations, compatibility facades and their retirement checkpoint: No non-English translations were added. `notice.indexedNodes` deliberately defines English `one` and `other` forms with identical legacy wording (`{count} nodes`) so L00 proves plural selection without silently changing current product copy; a later copy-review/translation checkpoint may choose a singular English form explicitly. The displayed hint advertises only the accepted Mod+F branch, while F4 remains supported and unchanged. No new compatibility facade was introduced; the pre-existing `src/ui/viewProfile.ts` compatibility facade is untouched and remains scheduled for retirement at C25 per the plan. The only verification deviations are the unavailable pinned Node 22.22.2/dependency-backed lint+build/Git metadata/live Obsidian lanes described above.
